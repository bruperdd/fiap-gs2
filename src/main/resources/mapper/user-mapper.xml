<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="br.com.fiap.pulsecheck.mapper.UserMapper">

    <insert id="createUser"
            parameterType="br.com.fiap.pulsecheck.model.Users"
            useGeneratedKeys="true"
            keyProperty="id">

        INSERT INTO users (
            email,
            password_hash,
            name,
            department_id,
            role,
            active
        )
        VALUES (
                   #{email},
                   #{password_hash},
                   #{name},
                   #{department_id},
                   #{role},
                   #{active}
               )
    </insert>

    <select id="findAll" resultType="br.com.fiap.pulsecheck.model.Users">
        SELECT id, department_id, email, name, role, password_hash, created_at, active
        FROM users
        ORDER BY created_at DESC
    </select>

    <select id="findById" parameterType="int" resultType="br.com.fiap.pulsecheck.model.Users">
        SELECT id, department_id, email, name, role, password_hash, created_at, active
        FROM users
        WHERE id = #{id}
    </select>

    <select id="findByEmail" parameterType="string" resultType="br.com.fiap.pulsecheck.model.Users">
        SELECT id, department_id, email, name, role, password_hash, created_at, active
        FROM users
        WHERE email = #{email}
    </select>

    <update id="updateUser" parameterType="br.com.fiap.pulsecheck.model.Users">
        UPDATE users
        SET name = #{name},
            email = #{email},
            role = #{role},
            department_id = #{department_id}
        WHERE id = #{id}
    </update>

    <update id="deactivateUser" parameterType="int">
        UPDATE users
        SET active = false
        WHERE id = #{id}
    </update>

    <update id="activateUser" parameterType="int">
        UPDATE users
        SET active = true
        WHERE id = #{id}
    </update>

</mapper>
